# AimTrainer Codespace Configuration

This Codespace:
- Installs Node 18 automatically
- Installs frontend dependencies with `npm install`
- Opens port 5173 for the Vite dev server
- Starts clean and ready for development

To run the app:
cd frontend
npm run dev
Your live app will appear in the forwarded ports tab.
